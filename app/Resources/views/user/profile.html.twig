{% block body %}
<nav style="background-color: #3e8f3e">


    <div >
        <div class="col-sm-4 col-sm-offset-4">
            <form action="{{ path('job_today', {id: user.id}) }}" method="get">
                <input type="submit" value="job to do"/>

            </form>


        </div>
    </div>
    <div class="form-group">
        <div class="col-sm-4 col-sm-offset-4">
            <a class="btn btn-default" href="{{ path('user_intern', {id: user.id}) }}">intern till now</a>
        </div>

        </div>
{#    <button href="{{ path('user_intern',{'user': user}) }}">intern till now</button>#}
    <button href="#">left rest days</button>
    <button href="#">aplication for furlough</button>
{#     <button href="{{ path('security_logout') }}">LOGOUT</button>#}
    <div class="col-sm-4 col-sm-offset-4">
        <a class="btn btn-default" href="{{ path('security_logout') }}">LOGOUT</a>
{#        <button type="submit" class="btn btn-success">Edit</button>#}
    </div>

</nav>
    <hr/>
   <p></p>{{ 'username ---->  '}} {{ user.userName }}<br/>{{ 'email-----> ' }}{{ user.email }}<p></p>
<p>{{ 'FURLOUGH---->' }}{{ user.furlough }}</p>{{ 'job for today---->' }}<p></p>
<small>{{ 'dataAdded----->' }}{{ user.dateAdded.format('d-m-y') }}</small>
    <p>{{ 'user_id ---> ' }}{{ user.id }}</p>

{% endblock %}

