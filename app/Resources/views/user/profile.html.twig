<body  style="background-color: #3e8f3e">
{% block body %}



{% if app.user and app.user.roles[0]=="ROLE_ADMIN" %}

            <form action="{{ path('furlough_index', {id: user.id}) }}"
                  method="get" >
                <input type="submit" value="all furlough"
                       style="width: 144px;background-color: #5eb5e0"/>
            </form>

    <form action="{{ path('user_all') }}" method="get" >
        <input type="submit" value="all users create plan" style="width: 144px;background-color: #ffcc00"/>
    </form>


            <form action="{{ path('plan_done', {id: user.id}) }}" method="get">
                <input type="submit" value="all undone plans" style="background-color: #AA3333; width: 144px "/>
            </form>

            <form action="{{ path('plan_index', {id: user.id}) }}" method="get" >
                <input type="submit" value="all plans"
                       style="width: 144px;background-color: #5eb5e0"/>
            </form>
    {% endif %}


            <form action="{{ path('job_todo', {id: user.id}) }}" method="get" >
                <input type="submit" value="job to do" style="width: 144px;background-color: #5eb5e0"/>
            </form>


            <form action="{{ path('user_intern', {id: user.id}) }}" method="get" >
                <input type="submit" value="Intern" style="width: 144px;background-color: #5eb5e0"/>
            </form>
            <form action="{{ path('furlough_new') }}" method="post" >
                <input type="submit" value="application for furlough" style="width: 144px;background-color: #5eb5e0"/>
            </form>
        <a class="btn btn-default" href="{{ path('security_logout') }}" style="width: 144px">LOGOUT</a>
        <hr/>

   {{ 'username ---->  '}} {{ user.userName }}<br/>{{ 'email-----> ' }}{{ user.email }}<p></p>
<p>{{ 'FURLOUGH---->' }}{{ user.furlough }}</p>{{ 'job for today---->' }}<p></p>
<small>{{ 'dataAdded----->' }}{{ user.dateAdded.format('d-m-y') }}</small>
    <p>{{ 'user_id ---> ' }}{{ user.id }}</p>
     <p>{{ "user ROLE ---->" }}{{ user.roles[0] }}</p>
{% endblock %}

</body>