{#{% extends 'base.html.twig' %}#}

{% block body %}
    <h1>Furlough creation for {{ user.userName }}</h1>
    <br/>
    <h1> YOU HAVE {{ app.user.furlough}} DAYS FURLOUGH</h1>
    <br/>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('furlough_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
